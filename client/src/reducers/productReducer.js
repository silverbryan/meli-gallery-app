import { SEARCH_RESULTS } from '../'
const initialState = {
    products =[],
    searchResults =[],
    productDetail = {},
};

export default productReducer(state = initialState, action) {
    switch (action.type) {
        case value:

            break;

        default:
            return { ...state };
    }
};